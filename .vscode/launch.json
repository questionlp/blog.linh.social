{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Jekyll Server",
            "type": "node-terminal",
            "request": "launch",
            "command": "bundle exec jekyll server --drafts --future --unpublished",
        }
    ]
}